/// Route path constants for the app shell (e.g. connecting screen).
abstract class AppShellRoutes {
  /// Path for the connecting screen shown while the app is not connected.
  static const String connecting = '/connecting';
}
