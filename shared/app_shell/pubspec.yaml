name: app_shell
description: Shared app shell with connection-aware AppBloc and redirect helper.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.11.0
  flutter: ^3.41.2

dependencies:
  bloc: ^9.0.0
  connection_repository:
    path: ../connection_repository
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  go_router: ^14.6.2

dev_dependencies:
  bloc_test: ^10.0.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  test: ^1.29.0
  very_good_analysis: ^10.0.0
